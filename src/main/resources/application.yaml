spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/appdrop
    username: ${username}
    password: ${password}

  jpa:
    show-sql: true
    properties:
      hibernate:
        format-sql: true
    hibernate:
      ddl-auto: create-drop